---
- name: Update server
  roles: [server]
  hosts: wordpress
  become: true

- name: Install php
  roles: [php]
  hosts: wordpress
  become: true

- name: Install mariadb
  roles: [mysql]
  hosts: wordpress
  become: true

- name: Install nginx
  roles: [nginx]
  hosts: wordpress
  become: true

- name: Install wordpress
  roles: [wordpress]
  hosts: wordpress
  vars:
    nginx_configured: true
    php_version: 8.3
  become: true
